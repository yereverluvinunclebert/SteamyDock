The VB6 utility requires the external dependency MSCOMCTL.OCX which is
included here. The installer will eventually, when complete, install the package
and MSCOMCTl.OCX if it isn't already registered.

The OCX provides access to:

Microsoft Windows Common Controls 6.0 SP3 mscomctl.ocx (treeview, slider)

