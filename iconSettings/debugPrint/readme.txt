This is the code that creates the separate utility that serves to provide a 
runtime debug window that is displayed when you run the main menu and select
the "turn debugging on" option. This utility is only needed for debugging.